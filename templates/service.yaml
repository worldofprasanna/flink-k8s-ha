apiVersion: v1
kind: Service
metadata:
  name: {{ include "flink-k8s-ha.fullname" . }}
  labels:
    {{- include "flink-k8s-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flink-k8s-ha.selectorLabels" . | nindent 4 }}
